import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tool.aistudio',
  appName: 'استوديو-تصوير-المنتجات-بالذكاء-الاصطناعي',
  webDir: 'dist'
};

export default config;
